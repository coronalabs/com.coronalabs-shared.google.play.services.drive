Sources for the plugin `shared.google.play.services.drive`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.google.play.services.drive" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
